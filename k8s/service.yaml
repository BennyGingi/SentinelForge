apiVersion: v1
kind: Service
metadata:
  name: collector
  namespace: sentinelforge
spec:
  type: NodePort
  selector:
    app: collector
  ports:
    - port: 8787
      targetPort: 8787
      nodePort: 30787
